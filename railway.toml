[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && apt-get update && apt-get install -y libreoffice poppler-utils"

[deploy]
startCommand = "uvicorn app:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
